version: "3.8"

# BackPocket OS — full stack
# Usage:
#   docker-compose up -d            # start everything
#   docker-compose logs -f api      # watch backend logs
#   docker-compose restart api      # hot-restart after code change
#
# Flutter web build must be done first:
#   cd flutter_prototype/backpocket_mobile && flutter build web --release
# Output lands in static_flutter/ (already gitignored for binaries).

services:

  # ── Python FastAPI backend ────────────────────────────────────────────────
  api:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file: .env
    volumes:
      # Persist the SQLite DB and uploads outside the container
      - ./backpocket.db:/app/backpocket.db
      - ./uploads:/app/uploads
      - ./documents:/app/documents
      # OAuth tokens (Gmail etc) stay on host
      - ./token.json:/app/token.json
      - ./token_ywa.json:/app/token_ywa.json
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - backpocket

  # ── Flutter web (nginx) ───────────────────────────────────────────────────
  web:
    image: nginx:alpine
    restart: unless-stopped
    volumes:
      - ./static_flutter:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "80:80"
    depends_on:
      - api
    networks:
      - backpocket

  # ── Paperless-ngx document management ────────────────────────────────────
  broker:
    image: docker.io/library/redis:7
    restart: unless-stopped
    volumes:
      - paperless_redis:/data
    networks:
      - backpocket

  paperless_db:
    image: docker.io/library/postgres:16-alpine
    restart: unless-stopped
    volumes:
      - paperless_pg:/var/lib/postgresql/data/
    environment:
      POSTGRES_DB: paperless
      POSTGRES_USER: paperless
      POSTGRES_PASSWORD: paperless
    networks:
      - backpocket

  paperless:
    image: ghcr.io/paperless-ngx/paperless-ngx:latest
    restart: unless-stopped
    depends_on:
      - paperless_db
      - broker
    ports:
      - "8010:8000"
    volumes:
      - paperless_data:/usr/src/paperless/data
      - paperless_media:/usr/src/paperless/media
      - ./paperless_export:/usr/src/paperless/export
      - ./paperless_consume:/usr/src/paperless/consume
    environment:
      PAPERLESS_REDIS: redis://broker:6379
      PAPERLESS_DBHOST: paperless_db
      PAPERLESS_DBNAME: paperless
      PAPERLESS_DBUSER: paperless
      PAPERLESS_DBPASS: paperless
      PAPERLESS_SECRET_KEY: ${PAPERLESS_SECRET_KEY:-backpocket-paperless-secret-change-in-prod}
      PAPERLESS_OCR_LANGUAGE: eng
      PAPERLESS_OCR_MODE: skip_noarchive
      PAPERLESS_DATE_ORDER: DMY
      PAPERLESS_TIME_ZONE: Australia/Sydney
      PAPERLESS_ALLOWED_HOSTS: "*"
      PAPERLESS_CORS_ALLOWED_HOSTS: "http://localhost,http://localhost:80,http://localhost:8000"
      PAPERLESS_ADMIN_USER: ${PAPERLESS_ADMIN_USER:-admin}
      PAPERLESS_ADMIN_PASSWORD: ${PAPERLESS_ADMIN_PASSWORD:-backpocket123}
      PAPERLESS_ADMIN_MAIL: ${PAPERLESS_ADMIN_MAIL:-admin@backpocket.local}
    networks:
      - backpocket

networks:
  backpocket:
    driver: bridge

volumes:
  paperless_redis:
  paperless_pg:
  paperless_data:
  paperless_media:
